const fs = require('fs');
const path = require('path');

const toolsPath = path.join(__dirname, 'src', 'toolsKnowledge.js');
let toolsContent = fs.readFileSync(toolsPath, 'utf8');

// The broken Three Horizons section
const brokenHorizons = `5. מידע תיאורטי מוסתר: חובה עליך לעטוף את ההסבר התיאורטי על המודל שמעל הטבלה בתגיות HTML של \`<details>
<summary><b>עקרונות לשימוש בכלי</b></summary>
<br>
<p>כשמתמודדים מול אדם שקשה לו, הגישה היא לא "להנחית" פתרונות אינסטנט. יש להבין לעומק מה קשה לו ולעודד אותו להגדיר בעצמו מה הוא צריך. ברגע שהוא מגדיר את הצורך, הוא הופך לשותף פעיל ולוקח בעלות על הפתרון.</p>
<p>תפקידך משתנה בהתאם לכובע שאתה חובש:</p>
<ul>
<li><b>כיועץ:</b> אתה מברר קודם כל מה הקושי והצורך של המנהלת עצמה.</li>
<li><b>כמאמן:</b> אתה מלמד ומתרגל יחד עם המנהלת כיצד לברר צרכים מול העובדים שלה.</li>
</ul>
<p><b>גבולות וצורך מערכתי:</b> לעיתים חובה על המנהלת גם להציג את הקושי המערכתי שלה ואת הצורך הבלתי מתפשר של בית הספר, לפני או אחרי שהיא מבררת את צרכי העובד.</p>
</details>
(הערה למודל: העתק את כל בלוק ה-<details> הזה מילה במילה, כולל כל תגיות ה-HTML, ה-<ul> וה-<li>. אל תנסה לנסח אותו מחדש ב-Markdown!)`;

const fixedHorizons = `5. מידע תיאורטי מוסתר: חובה עליך לעטוף את ההסבר התיאורטי על המודל שמעל הטבלה בתגיות HTML של \`<details>\` בדיוק לפי התבנית הבאה:
   <details><summary>מהו מודל שלושת האופקים? - הקישו כאן</summary>
   כלי המאפשר לזהות תבניות, לחשוב באופן שיטתי ולהכניס את עצמנו לתמונה הארגונית מול העתיד. המודל מורכב משלושה אופקים שמתקיימים כולם במקביל על ציר הזמן, אך הדומיננטיות שלהם משתנה.
   </details>`;

if (toolsContent.includes(brokenHorizons)) {
    toolsContent = toolsContent.replace(brokenHorizons, fixedHorizons);
    console.log("Fixed Horizons details tag");
} else {
    console.log("Could not find broken Horizons text");
}

// The old wrong curtain
const oldCurtain = `<details>
<summary><b>עקרונות לשימוש בכלי</b></summary>
<p>כשמתמודדים מול אדם שקשה לו, הגישה היא לא "להנחית" פתרונות אינסטנט.<br>
יש להבין לעומק מה קשה לו ולעודד אותו להגדיר בעצמו מה הוא צריך.<br>
ברגע שהוא מגדיר את הצורך, הוא הופך לשותף פעיל ולוקח בעלות על הפתרון.</p>
<p>תפקידך משתנה בהתאם לכובע שאתה חובש:<br>
- <b>כיועץ:</b> אתה מברר קודם כל מה הקושי והצורך של המנהלת עצמה.<br>
- <b>כמאמן:</b> אתה מלמד ומתרגל יחד עם המנהלת כיצד לברר צרכים מול העובדים שלה.</p>
<p><b>גבולות וצורך מערכתי:</b><br>
לעיתים חובה על המנהלת גם להציג את הקושי המערכתי שלה ואת הצורך הבלתי מתפשר של בית הספר, לפני או אחרי שהיא מבררת את צרכי העובד.</p>
</details>`;

const fixedCurtain = `<details>
<summary><b>עקרונות לשימוש בכלי</b></summary>
<p>כשמתמודדים מול אדם שקשה לו, הגישה היא לא "להנחית" פתרונות אינסטנט. יש להבין לעומק מה קשה לו ולעודד אותו להגדיר בעצמו מה הוא צריך. ברגע שהוא מגדיר את הצורך, הוא הופך לשותף פעיל ולוקח בעלות על הפתרון.</p>
<p>תפקידך משתנה בהתאם לכובע שאתה חובש:</p>
<ul>
<li><b>כיועץ:</b> אתה מברר קודם כל מה הקושי והצורך של המנהלת עצמה.</li>
<li><b>כמאמן:</b> אתה מלמד ומתרגל יחד עם המנהלת כיצד לברר צרכים מול העובדים שלה.</li>
</ul>
<p><b>גבולות וצורך מערכתי:</b> לעיתים חובה על המנהלת גם להציג את הקושי המערכתי שלה ואת הצורך הבלתי מתפשר של בית הספר, לפני או אחרי שהיא מבררת את צרכי העובד.</p>
</details>
(הערה למודל: העתק את כל בלוק ה-details הזה מילה במילה, כולל כל תגיות ה-HTML, ה-ul וה-li. אל תנסה לנסח אותו מחדש ב-Markdown!)`;

if (toolsContent.includes(oldCurtain)) {
    toolsContent = toolsContent.replace(oldCurtain, fixedCurtain);
    console.log("Fixed main tool curtain tag");
} else {
    console.log("Could not find old curtain text");
}

fs.writeFileSync(toolsPath, toolsContent, 'utf8');
